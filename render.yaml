services:
  - type: web
    name: ai-tennis-backend
    env: python
    repo: https://github.com/austinyannone1/ai-tennis-backend
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port 10000
    plan: free
    envVars: []
